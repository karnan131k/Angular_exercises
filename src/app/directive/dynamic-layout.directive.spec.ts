import { DynamicLayoutDirective } from './dynamic-layout.directive';

describe('DynamicLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
